name: CI - Backend API Docker Image

on:
  workflow_dispatch:
  
env:
  IMAGE_TAGS: nicholassiew1991/url-shortener-api:latest,nicholassiew1991/url-shortener-api:$(date +%Y%m%d%H%M%S)

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    
    - uses: actions/checkout@v3
    
    - uses: docker/login-action@v2.0.0
      name: Login to Docker Hub
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_PASSWORD }}
        
    - name: Echo Env
      run: echo $IMAGE_TAGS
      
    #- name: Build the Docker image
    #  run: docker build . -t url-shortener-api:$(date +%Y%m%d%H%M%S) -t url-shortener-api:latest
